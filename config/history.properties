# ── Historical Analytics Persistence Configuration ──────────────────────────
# Schema version: 1
# Freeze contract: do not change field names without bumping schemaVersion

# Retention: run snapshots (immutable source-of-truth)
history.retention.days=90
history.max.run.snapshots=500

# Retention: page performance append-log entries per page
history.max.perf.entries.per.page=200

# Rolling window for test stability recentHistory array
history.stability.window=20
